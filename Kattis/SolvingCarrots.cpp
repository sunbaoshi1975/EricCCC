#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, p;
    char desc[100];
    cin >> n >> p;
    for(int i = 0; i < n; i++) {
        cin >> desc;
    }
    cout << p;
    return 0;
}