#include <bits/stdc++.h>    // include all
using namespace std;

int main() {
    int N;
    scanf("%d\n", &N);
    while(N--) {
        char X[110];
        scanf("0.%[0-9]...\n", &X);
        printf("the digits are 0.%s\r\n", X);
    }
    return 0;
}
